name: Deploy to Docker Hub

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      DOCKER_USERNAME: codeiddih  # Set your Docker Hub username here

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Image Tag
        run: echo "IMAGE_TAG=${{ env.DOCKER_USERNAME }}/my-static-site:$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build Docker Image
        run: docker build -t $IMAGE_TAG .

      - name: Push Docker Image to Docker Hub
        run: |
          docker push $IMAGE_TAG
          docker tag $IMAGE_TAG ${{ env.DOCKER_USERNAME }}/my-static-site:latest
          docker push ${{ env.DOCKER_USERNAME }}/my-static-site:latest
